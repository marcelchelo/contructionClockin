<h1>Fill out to create your account.</h1>

<div class="container">
    <form  action="/user/newAccount"  method="POST">
        <div class="form-group">
            <label for="email" >Email</label>
            <input type="email"  maxLength="64" class="form-control" name="email" placeholder="youremail@company.com" value="{{email}}" required >
            <small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small>
        </div>
        
        <div class="form-group">
            <label for="password" >Password</label>
                <input type="password" class="form-control" name="password" placeholder="Password" value="{{password}}">
        </div>

        <div class="form-group">
            <label for="password2">Confirm Password</label>
                <input type="password" class="form-control" name="password2" placeholder="Password" value="{{password2}}">
        </div>
        <div class="form-group">
            <label for="accType">Which one are you?</label>
            <select class="form-control" name="accType">
                 <option>Business/Negocio</option>
                 <option>Employee/Empleado</option>
             </select>
        </div>

        <button type="submit" class="btn btn-primary"> Submit</button>
        
    </form>


</div>